server:
  port: 9002
spring:
  application:
    name: stampmarket-stamp

  datasource:
    url: jdbc:mysql://192.168.88.130:3306/stamporder?characterEncoding=UTF8&useSSL=false
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: 123456
  jpa:
    show-sql: true # 是否打印sql语句
    generate-ddl: true # 是否自动建表
    database: mysql # jpa连接的数据库类型
